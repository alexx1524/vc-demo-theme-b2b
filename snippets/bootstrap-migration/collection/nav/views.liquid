<div class="form-group">
    <ul class="nav nav-pills views"
        vc-query-target="$collection.viewQuery">
        <li class="nav-item">
            <a class="btn"
               ng-class="[$collection.viewQuery.view[0] === 'list' ? 'btn-primary': 'btn-outline-primary']"
               title="{{ 'collections.general.list_view' | t }}"
               vc-query-source="{ view: ['list'] }"
               switchable="false">
                <i class="fa fa-list-ul" aria-hidden="true"></i>
                <span class="ml-2">{{ 'collections.general.list_view' | t }}</span>
            </a>
        </li>
        <li class="nav-item ml-1">
            <a class="btn"
               ng-class="[$collection.viewQuery.view[0] === 'grid' ? 'btn-primary': 'btn-outline-primary']"
               title="{{ 'collections.general.grid_view' | t }}"
               vc-query-source="{ view: ['grid'] }"
               switchable="false">
                <i class="fa fa-th" aria-hidden="true"></i>
                <span class="ml-2">{{ 'collections.general.grid_view' | t }}</span>
            </a>
        </li>
    </ul>
</div>
